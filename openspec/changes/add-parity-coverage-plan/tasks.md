1. [x] **Period builders:** Wire `CarbonInterface` and `CarbonBase` to expose `centuriesUntil`, `daysUntil`, `decadesUntil`, `hoursUntil`, `microsUntil`, `microsecondsUntil`, `millenniaUntil`, `millisUntil`, `millisecondsUntil`, `minutesUntil`, `monthsUntil`, `quartersUntil`, `secondsUntil`, `weeksUntil`, and `yearsUntil`, then add regression tests that assert the returned `CarbonPeriod` spans the expected number of steps.
2. [x] **UTC diff helpers:** Add strongly typed wrappers for every `diffInUTC*` variant (centuries → years) plus unit tests comparing their results against known UTC math.
3. [x] **Long/short diffForHumans helpers:** Decide whether to implement `longRelative*`/`shortRelative*` wrappers or adjust parity expectations; document the decision and cover it with tests mirroring PHP Carbon.
